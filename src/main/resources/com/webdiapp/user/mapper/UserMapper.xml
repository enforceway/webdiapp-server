<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webdiapp.user.mapper.UserMapper">
    <select id="getList" resultType="User">
        select
        id,
        user_name as username,
        <include refid="SurveyCommonTemplateMapper.baseColumnsQry" />
        from user_t where use_yn!='n' limit #{skip},#{size}
    </select>
    <select id="getById" parameterType="INTEGER" resultType="User">
        select
        id,
        user_name as username,
        <include refid="SurveyCommonTemplateMapper.baseColumnsQry" />
        from user_t where id=#{id} and use_yn!='n'
    </select>
    <select id="getCount" resultType="int">
        select count(1) from user_t where use_yn != 'n'
        <if test="username != null and username != ''">
            user_name like '%#{username}%'
        </if>
    </select>
    <insert id="insert" parameterType="User">
        insert into
        user_t(user_name,<include refid="SurveyCommonTemplateMapper.baseColumnsInsertFields" />)
        values(
        #{username},
       	<include refid="SurveyCommonTemplateMapper.baseColumnsInsert" />
        )
    </insert>
    <delete id="delete">
        delete from user_t where id=#{id}
    </delete>
    <update id="update" parameterType="User">
        update user_t set
        user_name=#{username},
        lastupdate_timestamp=#{lastupdateTimestamp},
        lastupdate_user=#{lastupdateUser, javaType=INTEGER, jdbcType=INTEGER},
        lastupdate_username=#{lastupdateUsername, javaType=string, jdbcType=VARCHAR}
        where id=#{id}
    </update>
</mapper>